
# 기본 형태
def hello():
  print("안녕!!!")

hello()

print("-------------------------------")

# 매개변수(=입력값) 있는 함수
def hello(name):
  print(name, "안녕!!!")

hello("호빵맨")
hello("세균맨")

print("-------------------------------")

# 값 돌려주는 함수 (return)
def 더하기(a, b):
  return a + b

result = 더하기(3, 7)
print(result)

print("-------------------------------")

# 실습 미션
# 미션 1 - 나만의 인사 함수 만들기
def greet(name):
  print("👧🏻", name, "오늘도 화이팅!!!")

greet("호빵맨")

print("-------------------------------")

# 미션 2 - 점수 평균 계산기 함수 만들기
def get_avg(scores):
  total = 0
  for s in scores:
    total += s
  return total / len(scores)

print(get_avg([90, 80, 100]))

print("-------------------------------")

# 미션 3 - 비밀 메시지 암호화 함수
def secret(msg):
  return msg[::-1]  # 문자열 뒤집기

print(secret("호빵맨최고"))